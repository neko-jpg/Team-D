# ハッカソン開発原則

最終更新: 2026-08-31

## 1. 目的

本プロジェクトのゴールは、本番運用可能なシステムを構築することではない。4人のチームが2日間で、中心となるユーザー体験をローカル環境上の一連のデモとして完成させ、その価値を伝えられる状態にすることである。

実装方針や優先順位に迷った場合は、将来の拡張性や網羅性よりも、デモの完成と中心体験の分かりやすさを優先する。

## 2. 完成の定義

次の体験が、ローカル環境またはデモ用端末で最初から最後まで動作することを完成条件とする。

```text
写真を入力する
  ↓
AIから撮影内容と次の行動が返る
  ↓
必要な写真を揃える
  ↓
背景を変更する
  ↓
元画像と比較して承認する
```

本番環境へのデプロイ、長期運用、複数ユーザーへの対応は完成条件に含めない。

## 3. 実行環境と実機確認

アプリはReact + TypeScript + Viteで構築するモバイルWebとし、iPhoneやAndroidのブラウザから利用する。ネイティブアプリのビルドや配布は行わない。

画像AIや背景分離との接続には、ローカルで動作する必要最小限のPython FastAPIを使用する。LiveKit Agent workerとはprovider schemaと設定を共有し、別entrypointで起動する。フロントエンドからはViteの`/api` proxyを経由して接続し、APIキーをブラウザへ渡さない。

スマートフォンでの実機確認にはngrokの一時HTTPS URLを使用する。AIエージェントは、可能な範囲でVite、FastAPI、LiveKit Agent、ngrokを起動し、発行されたURLを取得してクリック可能なリンクとして提示する。利用者が手作業でURLを探したり転記したりすることを前提にしない。

ngrokの認証や起動条件が揃っていない場合は、その不足を明示する。実機接続がすぐに利用できない場合でも開発を止めず、PCブラウザと保存済み画像による確認を継続する。

## 4. コンポーネントを先に作る

撮影、Agentの案内、進捗表示、背景選択、比較・承認など、ユーザーが直接触れるコンポーネントを先に作る。

共通基盤や抽象化を完成させてから画面を作るのではなく、各コンポーネントが固定データやモックを使って独立して動く状態を先に作る。その後、コンポーネント同士を接続してデモの中心経路を完成させる。

4人が互いの作業完了を待たずに進められるよう、初期段階では共有実装を最小限にする。共通化が必要な場合も、コンポーネント間で受け渡す最小限のデータ形式や接続点だけを先に決める。

## 5. モックから実サービスへ置き換える

外部AI、背景分離、カメラ入力などの不確実性が高い機能は、最初から完全な接続や安定化を目指さない。

まず固定JSON、サンプル画像、固定マスクなどを使ってユーザー体験を完成させる。画面上の一連の体験が成立した後で、モックを実際のAIや画像処理へ段階的に置き換える。

実サービスとの接続に失敗した場合でも、それを理由に他のコンポーネントの開発を止めない。接続前後で同じ入力と出力を扱える、必要最小限の境界だけを用意する。

## 6. 抽象化は必要になってから行う

provider層、汎用的な状態管理、詳細なエラー分類、再利用可能なパイプラインなどは、将来必要になる可能性だけを理由に先行実装しない。

実際の接続によって重複や不安定さが確認された場合に限り、デモを安定させるために必要な範囲で抽象化する。抽象化そのものを完成物として扱わず、中心体験を動かすための手段として判断する。

## 7. リアルタイム処理は後段とする

リアルタイムのカメラ解析、AR表示、自動撮影、連続的なAI判定は、撮影後の画像を使った基本体験が完成した後に検討する。

まず「画像を撮影またはアップロードするとAIが判断して次の行動を返す」方式で中心体験を成立させる。リアルタイム化は中心体験を強化する追加要素であり、基本機能の完成を妨げてはならない。

リアルタイム処理を追加する場合も、既存の動作経路を置き換えてから作るのではなく、完成済みのコンポーネントへ段階的に接続する。

## 8. ローカルデモに不要な基盤は作らない

ハッカソンの実装では、次の要素を原則として追加しない。

- データベース
- ユーザー認証
- 永続セッション
- マルチユーザー、マルチテナント対応
- 本番向けデプロイ構成
- 監視、課金、運用管理
- 将来拡張だけを目的とした共通基盤
- 網羅的な自動テストや異常系テスト

ただし、APIキーをブラウザやリポジトリへ露出させないためのローカルAPIなど、デモを安全に動かすための最低限の境界は設ける。

テストやエラー処理は、デモの中心経路を壊しやすい箇所と、当日に復旧できない問題を優先する。仕様上考えられるすべての状態を網羅することは目的にしない。

## 9. 優先順位の判断基準

実装方針に迷った場合は、次の順に優先する。

1. ユーザーに見える中心体験が一連に動くこと
2. 4人が互いの完成を待たずに並行作業できること
3. デモ当日に安定して再現できること
4. 実サービスと接続すること
5. デモの失敗要因を限定的に安定化すること
6. 再利用性、汎用性、将来拡張
7. リアルタイム化、AR、自動化

将来の拡張性とデモの完成が競合する場合は、原則としてデモの完成を優先する。

## 10. 関連資料

- [企画要件定義](./requirements.md)
- [Webアーキテクチャ](./architecture.md)
- [OpenSpec変更提案](./openspec/changes/build-listing-photo-assistant-mvp/proposal.md)
